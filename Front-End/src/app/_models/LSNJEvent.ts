export interface EventDetails {
    id?: number;
    title: string;
    description: string;
    note: string;
    date: Date;
}

export interface EventHeader {
    id: number;
    title: string;    
    date: Date;
}